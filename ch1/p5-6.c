#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#define     MAX_INPUT 1000


int main( void ){
    //5.break修改为continue即可
    /*6.删掉下面这段
    if( num % 2 != 0){
        puts( "Last column number is not paired." );
        exit( EXIT_FAILURE );
    }
    将rearrange函数中for循环开头添加一行代码
    if( columns[col+1] == -1 )columns[col+1] = len;*/
    return EXIT_SUCCESS;
}